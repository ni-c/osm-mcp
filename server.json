{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ni-c/osm-mcp",
  "title": "OpenStreetMap",
  "description": "Geocoding, walking/driving/cycling distances, route optimization, isochrones and POI search on OSM",
  "websiteUrl": "https://osm-mcp.ni-c.de",
  "repository": {
    "url": "https://github.com/ni-c/osm-mcp",
    "source": "github"
  },
  "version": "0.1.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "osm-mcp",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "OSM_USER_AGENT",
          "description": "Identifying User-Agent sent to all OSM services (Nominatim requires one)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "ORS_API_KEY",
          "description": "Optional OpenRouteService key; switches routing/matrix/isochrones to ORS",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "ghcr.io/ni-c/osm-mcp:0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "OSM_USER_AGENT",
          "description": "Identifying User-Agent sent to all OSM services (Nominatim requires one)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "ORS_API_KEY",
          "description": "Optional OpenRouteService key; switches routing/matrix/isochrones to ORS",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        }
      ]
    }
  ]
}
